server:
  port: 8092
spring:
  application:
    name: config_client02
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8082/eureka